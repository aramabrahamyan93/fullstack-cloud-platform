name: CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled duplicate CI
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "This workflow is manual-only now."
          echo "Fullstack CI and Terraform CI handle automated checks."